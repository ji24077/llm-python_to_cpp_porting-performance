This is a simple paragraph of text that spans
across two source lines but stays one paragraph.

This is a second paragraph. It contains characters
that require HTML escaping such as 5 < 10, 10 > 5,
the ampersand & symbol, and a "quoted" phrase.

A third paragraph with a trailing sentence.
