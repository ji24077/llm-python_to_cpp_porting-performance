Here is a fenced code block:

```
def add(a, b):
    return a + b
```

And a fenced block with a language info string:

```python
x = 1 < 2 and 3 > 2
```

An inline link to [the example site](https://example.com/page).

A backslash escape keeps the asterisks literal: \*not emphasis\*.
